import {Button} from './Button';

export const Counter = () => {
    return (
        <div>Counter
            <div>
                <Button buttonTitle={"set"}/>
            </div>
        </div>
    )
}